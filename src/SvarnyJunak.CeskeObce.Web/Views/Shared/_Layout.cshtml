<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="seznam-wmt" content="hzWkgtDkMEf8FvIAdTETxFy8T5AE6xZ0" />
    @{
        var metaDescription = ViewData["MetaDescription"] as String;
        if (!String.IsNullOrEmpty(metaDescription))
        {
    <meta name="description" content="@metaDescription">
        }
    }
    @await RenderSectionAsync("metadata", required: false)
    <title>@ViewData["Title"]</title>

    <environment include="Development">
        <link rel="stylesheet" href="~/lib/pixabay-autocompleted/auto-complete.css" asp-append-version="true" />
        <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    </environment>
    <environment exclude="Development">
        <link rel="stylesheet" href="~/css/site.min.css" asp-append-version="true" />
    </environment>

    @RenderSection("head", required: false)
</head>
<body>
    <header>
        <a href="@Url.Action("Index", "Home" )"><h2>Obyvatelé Česka</h2></a>
        <nav>
            <a asp-area="" asp-controller="Home" asp-action="About">O aplikaci</a>
        </nav>
    </header>

    <div class="content">
        @RenderBody()
    </div>

    @await RenderSectionAsync("scripts", required: false)

    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r; i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date(); a = s.createElement(o),
                m = s.getElementsByTagName(o)[0]; a.async = 1; a.src = g; m.parentNode.insertBefore(a, m)
        })(window, document, 'script', 'https://www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-2758168-5', 'auto');
        ga('send', 'pageview');
    </script>
</body>
</html>
